(* Coursera Programming Languages, Homework 3, Provided Code *)

exception NoAnswer

datatype pattern = Wildcard
		 | Variable of string
		 | UnitP
		 | ConstP of int
		 | TupleP of pattern list
		 | ConstructorP of string * pattern

datatype valu = Const of int
	      | Unit
	      | Tuple of valu list
	      | Constructor of string * valu

fun g f1 f2 p =
    let 
	val r = g f1 f2 
    in
	case p of
	    Wildcard          => f1 ()
	  | Variable x        => f2 x
	  | TupleP ps         => List.foldl (fn (p,i) => (r p) + i) 0 ps
	  | ConstructorP(_,p) => r p
	  | _                 => 0
    end

(**** for the challenge problem only ****)

datatype typ = Anything
	     | UnitT
	     | IntT
	     | TupleT of typ list
	     | Datatype of string

(**** you can put all your code here ****)

(*1*)
fun only_capitals stringList =
    List.filter ( fn s => Char.isUpper(String.sub(s,0) ) ) stringList

(*2*)
fun longest_string1 stringList =
    foldl
	(fn (s,x) => if String.size s > String.size x then s else x)
	""
	stringList

(*3*)
fun longest_string2 stringList =
    foldl
	(fn (s,x) => if String.size s >= String.size x then s else x)
	""
	stringList

(*4*)
fun longest_string_helper f xs =
    foldl (fn(x,y) => if f( String.size(x), String.size (y) ) then y else x) ""
(*	
fun longest_string3 stringList =
    let
	val f = longest_string_helper (fn (s,x) => if String.size s > String.size x then s else x)
    in
	f stringList
    end
	
fun longest_string4 stringList =
    let
	val f = longest_string_helper (fn (s,x) => if String.size s >= String.size x then s else x)
    in
	f stringList
    end
*)
(*5*)
fun longest_capitalized stringList = 0
					 
(*6*)
fun rev_string string = 0

			    (*7*)
					 (*8*)
					 (*9*)
					 (*10*)
					 (*11*)
					 (*12*)
					
